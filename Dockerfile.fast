FROM debian:bookworm-slim
RUN apt-get update \
    && apt-get install -y --no-install-recommends ca-certificates \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY target/release/enose-cloud /app/enose-cloud
ENV HOST=0.0.0.0
ENV PORT=8080
EXPOSE 8080
CMD ["/app/enose-cloud"]
